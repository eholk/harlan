;; -*- scheme -*-

(module
 (fn main ()
     (let X (vector (vector 1)))
     (print (vector-ref (vector-ref (var X) 0) 0))
     (assert (= 1 (vector-ref (vector-ref (var X) 0) 0)))
     (return 0)))
