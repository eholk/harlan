(module
  (import graphics)
  
  (define (main)
    (let ((img (kernel ((i (iota 1024)))
                 (kernel ((j (iota 1024)))
                   (mod (* i j) 256)))))
      (println (length img))
      (println (length (vector-ref img 0)))
      (write-pgm "test.bin/test-image.pgm" img)
      0)))
