(module
  (define (magnitude x y)
    (sqrt (+ (* x x) (* y y))))

  (define (main)
    (let ((xs (vector 1 2 3 4))
          (ys (vector 4 3 2 1)))
      (println (kernel ((x xs) (y ys))
                 (magnitude x y))))
    (return 0)))
