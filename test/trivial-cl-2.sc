;; -*- scheme -*-

;; Simple file showing the use of global variables, and ensuring we
;; can represent stuff from CL++.

((func int main ()
       (print ((field g_devices count)))
       (return 0)))
