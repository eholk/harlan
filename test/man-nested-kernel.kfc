;; An example from the Harlan manual. We want to make sure these all
;; run.

(module
  (define (main)
    (print
     (kernel ((a (vector 2 3)))
       (kernel ((b (vector 5 7)))
         (* a b))))
    (return 0)))

