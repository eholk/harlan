;; -*- scheme -*-

;; code for adding two vectors, from Harlan proposal

(module
  (define (main)
    (let ((X (vector 1 2 3 4))
          (Y (vector 5 1 1 7)))
      (kernel ((x X) (y Y)) (+ x y))
      (return 0))))
