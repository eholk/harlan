(module
  (define (main)
    (let ((x (vector (vector 1 2)
                     (vector 3 4))))
      (let ((y (kernel ((row x))
                 (kernel ((i row))
                   (+ i 1)))))
        (println y)
        (assert (= y (vector (vector 2 3)
                             (vector 4 5)))))
      (return 0))))
