;; Best Harlan bug ever:
;; [@hivequeen harlan (master)] ./print-if
;; found 1 devices
;; Creating queue for GeForce GTX 460
;; Right answer
;; Wrong answer

(module
  (define (main)
    (if (if (< 5 6)
            (begin (print "Right answer\n") #t)
            (begin (print "Wrong answer\n") #f))
        0
        1)))
