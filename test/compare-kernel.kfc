(module
  (define (main)
    (let ((A (kernel ((i (iota 10))) (* i i)))
          (B (kernel ((j (iota 10)))
                     (let ((r 1))
                       (while (> j 0)
                         (set! r (* r 2))
                         (set! j (- j 1)))
                       r))))
      (println (kernel ((a A) (b B)) (= a b))))
    (return 0)))
